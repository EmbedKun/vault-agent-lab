# Judge artifacts

`deploy_vault_lab.py` and `setup_assets_and_mission.py` generate answer keys here during a real run.

Do not commit generated `answer_key_*.json` files. They contain proof tokens and scoring secrets.

